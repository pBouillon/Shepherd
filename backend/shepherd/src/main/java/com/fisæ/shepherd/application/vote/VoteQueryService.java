package com.fisæ.shepherd.application.vote;

/**
 * Define a service able to handle the read-only operations (queries) performed on votes
 *
 * @see VoteService
 */
public interface VoteQueryService {
}
